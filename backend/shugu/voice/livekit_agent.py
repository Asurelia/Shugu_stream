"""LiveKit Agents Python worker — Shugu voice realtime.

Runs as a separate process subscribing to a LiveKit room.
Pipeline: STT (whisper.cpp Vulkan) → LLM (Ollama Gemma 4) → TTS (Piper).

Will be wired in Sprint B.
"""
from __future__ import annotations

# Implementation arrives in Sprint B
